# إصلاح سريع لمشكلة الاتصال بالسيرفر

## الخطوات السريعة (5 دقائق)

### 1. تحديث ملف `.env` في Laravel

افتح ملف `.env` في مشروع Laravel وابحث عن السطر:
```env
SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1
```

غيّره إلى:
```env
SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1,10.0.2.2
```

### 2. تشغيل السيرفر بشكل صحيح

**توقف عن السيرفر الحالي** (اضغط Ctrl+C) ثم شغّله بهذا الأمر:

```bash
php artisan serve --host=0.0.0.0 --port=8000
```

**⚠️ مهم جداً:** استخدم `--host=0.0.0.0` وليس فقط `php artisan serve`

### 3. مسح الكاش

بعد تحديث `.env`، نفّذ:

```bash
php artisan config:clear
php artisan cache:clear
```

### 4. إعادة تشغيل السيرفر

أعد تشغيل السيرفر مرة أخرى:

```bash
php artisan serve --host=0.0.0.0 --port=8000
```

---

## التحقق من أن كل شيء يعمل

### اختبار 1: من المتصفح
افتح المتصفح واذهب إلى:
```
http://localhost:8000/api/register
```

يجب أن ترى استجابة (حتى لو كانت خطأ، المهم أن السيرفر يرد).

### اختبار 2: من Flutter
شغّل التطبيق Flutter مرة أخرى. يجب أن يعمل الآن!

---

## إذا لم يعمل بعد

### تحقق من CORS

افتح `config/cors.php` وتأكد من:

```php
'allowed_origins' => ['*'],
'supports_credentials' => true,
```

### تحقق من Logs

افتح `storage/logs/laravel.log` لرؤية الأخطاء إن وجدت.

---

## ملخص سريع

```bash
# 1. حدّث .env
SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1,10.0.2.2

# 2. مسح الكاش
php artisan config:clear
php artisan cache:clear

# 3. شغّل السيرفر
php artisan serve --host=0.0.0.0 --port=8000
```

**هذا كل شيء!** 🎉



