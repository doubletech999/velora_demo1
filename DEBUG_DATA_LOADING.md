# 🔍 دليل استكشاف مشكلة عدم جلب البيانات

## المشكلة
التطبيق لا يجلب لا مواقع ولا مسارات من API.

## الحلول المطبقة

### 1. تحسين PathsProvider.loadPaths()
- ✅ إضافة معالجة أفضل للأخطاء
- ✅ تحميل `_sites` و `_routesAndCamping` من الـ cache عند فشل API
- ✅ إضافة logging مفصل لتتبع المشكلة

### 2. تحسين HomeScreen
- ✅ تحميل البيانات عند بدء التطبيق
- ✅ إعادة تحميل البيانات إذا كانت فارغة
- ✅ إضافة logging لتتبع حالة البيانات

### 3. تحسين FeaturedRoutesSection و FeaturedSitesSection
- ✅ إظهار مؤشر تحميل أثناء جلب البيانات
- ✅ إظهار رسالة بدلاً من إخفاء القسم عند عدم وجود بيانات
- ✅ محاولة تحميل البيانات تلقائياً

## خطوات التشخيص

### 1. تحقق من Console Logs
ابحث عن هذه الرسائل في console:
```
🔄 PathsProvider: بدء جلب البيانات...
🔄 جلب الأماكن السياحية (sites) من API...
🔄 جلب المسارات والتخييمات من API...
✅ PathsProvider: تم جلب X مكان سياحي
✅ PathsProvider: تم جلب X مسار/تخييم
```

### 2. تحقق من API Connection
- تأكد من أن Laravel server يعمل: `php artisan serve --host=0.0.0.0 --port=8000`
- تحقق من أن `baseUrl` في `ApiService` صحيح
- تحقق من أن CORS مُعد بشكل صحيح

### 3. تحقق من API Response
- افتح Network tab في DevTools
- تحقق من أن `/api/sites?type=site` يرجع بيانات
- تحقق من أن `/api/sites?type=route` يرجع بيانات
- تحقق من أن `/api/sites?type=camping` يرجع بيانات

### 4. تحقق من Cache
إذا فشل API، يتم تحميل البيانات من cache:
- تحقق من أن البيانات موجودة في cache
- تحقق من أن البيانات في cache صحيحة

## الحلول الإضافية

### إذا كانت المشكلة في API Connection:
```dart
// في ApiService، تأكد من أن baseUrl صحيح
String get baseUrl {
  if (kIsWeb) {
    return 'http://localhost:8000/api';
  } else {
    return 'http://10.0.2.2:8000/api';
  }
}
```

### إذا كانت المشكلة في CORS:
في Laravel `.env`:
```
SANCTUM_STATEFUL_DOMAINS=localhost:3000,127.0.0.1:3000
```

### إذا كانت المشكلة في Authentication:
- تأكد من أن `/api/sites` لا يحتاج authentication
- أو تأكد من إرسال Bearer Token بشكل صحيح

## اختبار سريع

قم بتشغيل هذا الكود في console:
```dart
final pathsProvider = Provider.of<PathsProvider>(context, listen: false);
await pathsProvider.loadPaths();
print('Paths: ${pathsProvider.paths.length}');
print('Sites: ${pathsProvider.sites.length}');
print('Routes: ${pathsProvider.routesAndCamping.length}');
```

## إذا استمرت المشكلة

1. تحقق من logs في console
2. تحقق من Network requests
3. تحقق من API response format
4. تحقق من PathModel.fromJson() parsing

